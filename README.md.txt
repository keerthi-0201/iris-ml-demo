# Iris ML Demo 🌸

A simple machine learning classification demo using the classic Iris dataset.

## What it does
- Loads Iris dataset
- Splits into train/test
- Trains Logistic Regression model with scaling
- Prints Accuracy + Classification Report

## Run locally
```bash
pip install -r requirements.txt
python app.py